import {StyleSheet} from 'react-native';
const task = StyleSheet.create({
  container: {
    alignItems: 'center',
    flex: 1,
  },
  btnContainer: {
    position: 'absolute',
    bottom: 15,
  },
});
export default task;
